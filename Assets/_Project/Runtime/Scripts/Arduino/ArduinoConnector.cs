using System;
using UnityEngine;
/*using System.IO.Ports;

public class ArduinoConnector : MonoBehaviour
{
    SerialPort _arduino = new SerialPort("COM4", 9600);
    [SerializeField] private int _readTimeout = 200;

    private void Start()
    {
        _arduino.Open();
        _arduino.ReadTimeout = _readTimeout;
    }

    private void Update()
    {
        string message = _arduino.ReadLine();
        //int value = int.Parse(message);
        Debug.Log(message);
    }
}*/
